{
  "name": "rules-please",
  "private": true,
  "workspaces": [
    "web",
    "mobile",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace web",
    "dev:mobile": "npm run start --workspace mobile",
    "android": "npm run android --workspace mobile",
    "build:web": "npm run build --workspace web",
    "check": "npm run typecheck --workspace @rulesplease/shared && npm run typecheck --workspace mobile && npm run typecheck --workspace web",
    "test": "npm run test --workspace @rulesplease/shared && npm run test --workspace mobile && npm run test --workspace web"
  },
  "devDependencies": { "@types/react": "19.2.17" }
}
