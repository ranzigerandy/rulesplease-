# Project instructions

- Unless the user explicitly says otherwise, implement product and interface changes in both the web application and the mobile app.
- Unless the user explicitly says otherwise, merge completed changes into `main` and publish them there.
